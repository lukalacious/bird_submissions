{
  "buildCommand": "npx prisma generate && npx prisma migrate deploy && next build",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/elimination-check",
      "schedule": "0 6 1 * *"
    },
    {
      "path": "/api/cron/process-form-jokers",
      "schedule": "0 5 * * *"
    }
  ]
}
